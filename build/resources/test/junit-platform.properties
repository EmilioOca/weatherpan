# each test method runs on the same test instance, 
# developers should take care method nad class set up and tear down
# Per_Class Advantages: tests run 25% faster, Developers are aware of setUp/tearDown
junit.jupiter.testinstance.lifecycle.default = per_class

#junit.jupiter.execution.parallel.enabled=true
#junit.jupiter.execution.parallel.mode.default=same_thread
#junit.jupiter.execution.parallel.config.fixed.parallelism=20
#junit.jupiter.execution.parallel.config.strategy=fixed